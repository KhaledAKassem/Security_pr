<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="medic.esy.es.security_project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sss"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splach_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Des_Activity" />
        <activity android:name=".jefferson_Activity" />
        <activity android:name=".RSA_Activity" />
        <activity android:name=".Transposition" />
        <activity android:name=".AFFINE" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>